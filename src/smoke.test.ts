import { describe, expect, it } from "vitest";

/**
 * Trivial smoke test so `npm test` has something to run while the simulation
 * core is scaffolded. Replace/expand with real simulation tests per docs/07-tech.md.
 */
describe("smoke", () => {
  it("runs the test suite", () => {
    expect(1 + 1).toBe(2);
  });
});
